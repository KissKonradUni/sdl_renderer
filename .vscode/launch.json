{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "(lldb) Build and debug",

            "type": "lldb",
            "request": "launch",
            "cwd": "${fileDirname}",
            "args": [],

            "windows": {
                "program": "${workspaceFolder}/build/sdl3_app.exe",
            },
            "linux": {
                "program": "${workspaceFolder}/build/sdl3_app.out",
            },
            
            "program": "${workspaceFolder}/build/sdl3_app",
            "preLaunchTask": "build"
        }
    ],
}